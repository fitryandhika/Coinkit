=====================================================================
PENTING — DIBACA DULU SEBELUM COMMIT
=====================================================================

Zip ini cuma bisa MENAMBAH/MENIMPA file lewat `unzip -o`. File-file
di bawah ini SUDAH DIHAPUS dari kode, tapi masih tertinggal di folder
lokal kamu — harus dihapus MANUAL, kalau tidak jadi kode mati dan
build bisa gagal karena import yang tidak ada.

Jalankan blok ini di Termux, di folder ~/work/cryptoai,
SETELAH unzip tapi SEBELUM git commit:

---------------------------------------------------------------------
# Fitur jurnal trading manual dibuang (tidak dipakai lagi)
rm -f  lib/db/manualTrades.js
rm -rf app/api/manual-trades
rm -rf "app/api/predictions/[id]/action"
---------------------------------------------------------------------

Cek tidak ada sisa referensi:

    grep -rn "manualTrade\|manual-trades\|updateUserAction" \
      --include=*.js app components lib hooks

Kalau tidak keluar apa-apa, berarti bersih.

=====================================================================
MIGRASI DATABASE — WAJIB
=====================================================================

Jalankan di Supabase SQL Editor:

    db/migration_003_calibration.sql

PERHATIAN: migrasi ini MENGHAPUS tabel `manual_trades` dan kolom
`user_action` secara permanen. Backup dulu kalau ada data yang masih
ingin disimpan.

Kalau database kamu belum pernah dapat migrasi 002, jalankan
`db/migration_002_trailing_sl.sql` lebih dulu.

=====================================================================
SETELAH DEPLOY
=====================================================================

1. Buka halaman "Kalibrasi" (dulu "Screener History").
   Wajar kalau tertulis "DATA BELUM CUKUP" — vonis baru muncul
   setelah 40 setup selesai dievaluasi.

2. Sub-score baru mulai tercatat untuk setup SETELAH migrasi.
   Tabel "Kontribusi Komponen" akan kosong dulu, itu normal.

3. Pastikan cron worker evaluate-predictions masih jalan. Tanpa itu
   tidak ada setup yang pernah selesai dievaluasi, dan halaman
   Kalibrasi akan kosong selamanya.

4. Opsional: `recharts` sudah tidak dipakai sama sekali. Kalau mau
   bundle lebih kecil: npm uninstall recharts
