=====================================================================
UPDATE: Perbaikan Tampilan Kalibrasi + Export CSV
=====================================================================

Update ini TIDAK menghapus file apa pun dan TIDAK butuh migrasi
database baru. Cukup unzip lalu commit.

    cd ~/work/cryptoai
    unzip -o ~/storage/downloads/Coinkit-update-2.zip
    npm test          # harus 53 pass, 0 fail
    git add -A
    git commit -m "perbaikan tampilan kalibrasi + export CSV"
    git push

---------------------------------------------------------------------
ISI UPDATE
---------------------------------------------------------------------

1. Tabel tidak lagi menumpuk di kiri.
   Penyebabnya .market-table dipaksa display:block (supaya bisa
   di-scroll), efek sampingnya tabel berhenti melebar. Halaman
   kalibrasi sekarang pakai .calib-table di dalam pembungkus
   .table-scroll.

2. Kolom "Expectancy" dibuang.
   Nilainya SELALU sama persis dengan Avg R secara matematis, jadi
   hanya memakan ruang layar. Diganti Avg MFE.

3. Angka 0R tidak lagi berwarna hijau. Nol bukan untung.

4. Label hasil dipendekkan: SL_HIT -> "Stop Loss",
   TRAILING_STOP_HIT -> "Trailing Stop", dst.

5. Kolom "Status" di tabel komponen dipindah ke bawah nama komponen
   supaya tidak terpotong di layar sempit.

6. Tabel "Per Arah" / "Per Timeframe" / "Per Jenis Hasil" sekarang
   punya judul kolom pertama (dulu kosong).

7. Ringkasan memakai kartu (tile), bukan baris teks berdempetan.

8. BARU: tombol "Export CSV" di header Riwayat Setup.
   Endpoint: /api/predictions/export
   Isi: 41 kolom - prediction + snapshot + outcome + realized R,
   TERMASUK control group. Sudah ber-BOM supaya Excel membaca UTF-8,
   dan escaping koma/kutip/newline sudah diuji.

=====================================================================
CATATAN
=====================================================================

File CSV berisi sub-score (momentum_score, volume_score, dst).
Untuk setup yang dicatat SEBELUM migrasi 003, kolom itu kosong.
Itu normal - bukan bug.
